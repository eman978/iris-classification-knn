# iris-classification-knn
Supervised learning project that classifies Iris flowers using K-Nearest Neighbors (KNN). Includes data preprocessing, train-test split, model evaluation (confusion matrix, F1 score), a full Jupyter notebook, and a deployed Streamlit web app for live predictions.
